# Pipeline di aggiornamento settimanale — giurisprudenza-db (settori penale e civile)
# Cron: lunedì 06:17 UTC — minuto off-peak: sul minuto zero GitHub ritarda
# o salta i job schedulati (verificato 13/7/2026: cron 06:00 mai partito).
# Trigger manuale: workflow_dispatch con dry_run (default true) e force.
name: Aggiorna banca dati

on:
  schedule:
    - cron: "17 6 * * 1"
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Solo log, nessun commit"
        type: boolean
        default: true
      force:
        description: "Rigenera anche le schede già presenti"
        type: boolean
        default: false
      backfill:
        description: "Backfill: scarica tutto l'archivio storico della Corte"
        type: boolean
        default: false

permissions:
  contents: write
  issues: write

jobs:
  aggiorna:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Dipendenze
        run: pip install --quiet requests beautifulsoup4 pdfplumber

      - name: Pronunce segnalate — settore PENALE
        run: |
          ARGS=""
          if [ "${{ inputs.dry_run }}" = "true" ]; then ARGS="$ARGS --dry-run"; fi
          if [ "${{ inputs.force }}" = "true" ]; then ARGS="$ARGS --force"; fi
          if [ "${{ inputs.backfill }}" = "true" ]; then ARGS="$ARGS --backfill --max-schede 600"; fi
          echo "Argomenti: $ARGS"
          python scripts/aggiorna_banca_dati.py --materia penale $ARGS

      - name: Pronunce segnalate — settore CIVILE
        run: |
          ARGS=""
          if [ "${{ inputs.dry_run }}" = "true" ]; then ARGS="$ARGS --dry-run"; fi
          if [ "${{ inputs.force }}" = "true" ]; then ARGS="$ARGS --force"; fi
          if [ "${{ inputs.backfill }}" = "true" ]; then ARGS="$ARGS --backfill --max-schede 600"; fi
          python scripts/aggiorna_banca_dati.py --materia civile $ARGS

      - name: Rinvii pregiudiziali art. 363-bis (civile)
        run: |
          ARGS=""
          if [ "${{ inputs.dry_run }}" = "true" ]; then ARGS="$ARGS --dry-run"; fi
          if [ "${{ inputs.force }}" = "true" ]; then ARGS="$ARGS --force"; fi
          if [ "${{ inputs.backfill }}" = "true" ]; then ARGS="$ARGS --backfill"; fi
          python scripts/rinvii_pregiudiziali.py $ARGS

      - name: Rassegne mensili civili (non bloccante)
        run: |
          ARGS=""
          if [ "${{ inputs.dry_run }}" = "true" ]; then ARGS="$ARGS --dry-run"; fi
          python scripts/rassegne_civili.py $ARGS || echo "::warning::rassegne civili fallite (non bloccante)"

      - name: Radar civile (non bloccante)
        run: |
          ARGS=""
          if [ "${{ inputs.dry_run }}" = "true" ]; then ARGS="--dry-run"; fi
          python scripts/radar_civile.py $ARGS || echo "::warning::radar civile fallito (non bloccante)"

      - name: Radar merito (non bloccante)
        run: |
          ARGS=""
          if [ "${{ inputs.dry_run }}" = "true" ]; then ARGS="--dry-run"; fi
          python scripts/radar_merito.py $ARGS || echo "::warning::radar merito fallito (non bloccante)"

      - name: Corte costituzionale (open data)
        run: |
          ARGS=""
          if [ "${{ inputs.dry_run }}" = "true" ]; then ARGS="--dry-run"; fi
          if [ "${{ inputs.force }}" = "true" ]; then ARGS="$ARGS --force"; fi
          python scripts/consulta.py $ARGS

      - name: Annuario Consulta (non bloccante)
        run: |
          ARGS=""
          if [ "${{ inputs.dry_run }}" = "true" ]; then ARGS="--dry-run"; fi
          python scripts/annuario.py $ARGS || echo "::warning::annuario fallito (non bloccante)"

      - name: Commit e push
        if: ${{ inputs.dry_run != true }}
        run: |
          git config user.name "synthos-bot"
          git config user.email "synthos-logic@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "Nessuna modifica da committare."
          else
            git commit -m "data: aggiornamento pronunce segnalate penali e civili ($(date -u +%F))"
            git push
          fi

      - name: Apri issue in caso di errore
        if: failure()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh issue create \
            --repo "${{ github.repository }}" \
            --title "Pipeline fallita — $(date -u +%F)" \
            --body "La run ${{ github.run_id }} è fallita. Log: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }} — Regola: in caso di anomalia la pipeline non inventa contenuti; verificare LOG_ERRORI.md e la struttura della pagina della Corte." \
            || echo "issue non creata (forse già esistente)"
